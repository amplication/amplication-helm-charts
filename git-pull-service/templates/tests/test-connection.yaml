apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "git-pull-service.fullname" . }}-test-connection"
  labels:
    {{- include "git-pull-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "git-pull-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
