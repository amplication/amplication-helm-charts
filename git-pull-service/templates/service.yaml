apiVersion: v1
kind: Service
metadata:
  name: {{ include "git-pull-service.fullname" . }}
  labels:
    {{- include "git-pull-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port.http }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.port.https }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "git-pull-service.selectorLabels" . | nindent 4 }}
