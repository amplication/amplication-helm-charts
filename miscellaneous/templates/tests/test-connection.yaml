apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "miscellaneous.fullname" . }}-test-connection"
  labels:
    {{- include "miscellaneous.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "miscellaneous.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
