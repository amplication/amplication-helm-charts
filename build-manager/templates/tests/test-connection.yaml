apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "build-manager.fullname" . }}-test-connection"
  labels:
    {{- include "build-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "build-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
