apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plugin-api.fullname" . }}-test-connection"
  labels:
    {{- include "plugin-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plugin-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
