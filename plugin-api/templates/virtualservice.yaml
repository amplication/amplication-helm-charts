{{- if .Values.environmentVariables.configMap }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "plugin-api.fullname" . }}
  labels:
    {{- include "plugin-api.labels" . | nindent 4 }}
spec:
  hosts:
  - {{ .Values.istio.hostname }}
  gateways:
  - {{ .Values.istio.gatewayName }}
  http:
  - name: http
    route:
    - destination:
        host: {{ include "plugin-api.fullname" . }}
        port:
          number: 80
  - name: https
    route:
    - destination:
        host: {{ include "plugin-api.fullname" . }}
        port:
          number: 443
{{- end }}