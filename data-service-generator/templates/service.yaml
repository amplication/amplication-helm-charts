apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-service-generator.fullname" . }}
  labels:
    {{- include "data-service-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-service-generator.selectorLabels" . | nindent 4 }}
