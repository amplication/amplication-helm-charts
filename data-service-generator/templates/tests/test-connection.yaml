apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-service-generator.fullname" . }}-test-connection"
  labels:
    {{- include "data-service-generator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-service-generator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
