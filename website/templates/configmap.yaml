{{- if .Values.environmentVariables.configMap }}
apiVersion: v1
kind: ConfigMap
  name: {{ include "website.fullname" . }}
  labels:
    {{- include "website.labels" . | nindent 4 }}
data: 
  {{- toYaml .Values.environmentVariables.configMap | nindent 2 }}
{{- end }}
