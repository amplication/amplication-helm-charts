apiVersion: v1
kind: Service
metadata:
  name: {{ include "git-push-webhoook-service.fullname" . }}
  labels:
    {{- include "git-push-webhoook-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "git-push-webhoook-service.selectorLabels" . | nindent 4 }}
