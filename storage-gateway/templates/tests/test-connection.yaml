apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "storage-gateway.fullname" . }}-test-connection"
  labels:
    {{- include "storage-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "storage-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
